# ============================================================
# Research Note Generator - Environment Variables
# ============================================================
# 이 파일을 .env로 복사한 후 값을 채워주세요:
#   cp .env.example .env
#
# .env 파일은 .gitignore에 포함되어 있어 커밋되지 않습니다.
# ============================================================

# Email (Gmail SMTP) - 이메일 알림 사용 시
# Gmail App Password 생성 방법:
#   1. Google 계정 → 보안 → 2단계 인증 활성화
#   2. Google 계정 → 보안 → 앱 비밀번호 → 생성
#   3. 생성된 16자리 비밀번호를 아래에 입력
SMTP_SENDER="your-email@gmail.com"
SMTP_PASSWORD="xxxx xxxx xxxx xxxx"

# Anthropic API (ai_backend: "anthropic_api" 사용 시)
# https://console.anthropic.com 에서 API key 발급
# ANTHROPIC_API_KEY="sk-ant-..."

# Slack Bot Token (Slack 알림 사용 시)
# https://api.slack.com/apps 에서 Bot Token 발급
# SLACK_BOT_TOKEN="xoxb-..."
